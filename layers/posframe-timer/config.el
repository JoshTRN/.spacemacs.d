;;; config.el --- posframe-timer layer configuration -*- lexical-binding: t; -*-

;; Set any `posframe-timer-*' defcustom in `dotspacemacs/user-config'.
;; Notables: `posframe-timer-default-duration' (seconds, default 30 min),
;; `posframe-timer-use-posframe' (nil to always use the mode-line clock
;; slot), `posframe-timer-org-clock-integration' (nil to stop mirroring
;; the org clock), `posframe-timer-timesheet-file' (where free clocks are
;; filed, default ~/org/timesheet.org), and the `posframe-timer-face'
;; `:height' for the display scale.

;;; config.el ends here
