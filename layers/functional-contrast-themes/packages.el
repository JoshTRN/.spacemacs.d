;; -*- lexical-binding: t -*-

;;; packages.el --- functional-contrast-themes layer packages file for Spacemacs

;; The themes are plain files in this layer's themes/ directory, not packages,
;; so nothing is installed. `custom-theme-load-path' is what makes them
;; loadable, and that is set in `dotspacemacs/user-init' -- see config.el for
;; why it cannot be done from here.

(defconst functional-contrast-themes-packages
  '()
  "The list of Lisp packages required by the functional-contrast-themes layer.")
