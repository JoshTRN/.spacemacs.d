#+TITLE: zoho-tickets layer

* Description
Zoho Desk ticketing inside Emacs, via the Zoho Desk REST API
(no browser involved).

** Features:
  - Dashboard: views sidebar on the left with =[x]= checkboxes (OR
    filter — the table shows the union of all checked views), ticket
    table on the right. Starred views are listed first, every other
    view below; on first open only the starred =Open= view is checked
  - Tickets open as org-mode documents with Overview / Thread /
    Comments / Time Logs tabs; each email, comment and time log is
    an org subheading
  - Incoming HTML (thread bodies, descriptions, comments) is
    converted to org markup via pandoc, so bold, tables, code
    blocks, lists and links keep their formatting on ingest
    (plain-text fallback when pandoc is not installed; see
    =zoho-desk-pandoc-program=)
  - Inline images in incoming emails are fetched through the API
    in the background and displayed in place in the ticket buffer
    (=zoho-desk-inline-images=, scaled to
    =zoho-desk-inline-image-max-width= pixels)
  - Outgoing replies send images *truly inline* (real cid MIME
    parts, not attachments): pasted screenshots via the composer
    servlet (=zoho-desk-send-inline-images=; needs the browser
    session cookie, refreshed on demand), quoted thread images
    over plain OAuth
  - All API traffic runs in the background — Emacs never blocks on
    the network. Fetches show a =Fetching …= placeholder that fills
    in when the API answers; replies, comments and time entries send
    while you keep working, and a failed send pulls you back: the
    buffer with the unsent content is refocused with a loud error,
    ready to retry
  - Post private/public comments
  - Post time entries to tickets — from a prompt anywhere, or from
    the ticket's Time Logs tab (a =** New Time Log= entry with
    Reply-style input fields sits at the top of the list)
  - Send org-clocked time to a ticket via the =ZOHO_TICKET_ID= property
  - Track work live with the posframe timer: ~T~ on a ticket clocks
    in a floating count-up labeled with the ticket number and
    subject; finishing it from anywhere drops the clocked start and
    end times into that ticket's New Time Log fields, ready to
    describe and submit

* Install
** Layer
Add =zoho-tickets= to =dotspacemacs-configuration-layers= in your
=~/.spacemacs=.

Optional: install =pandoc= (system package) so incoming HTML is
rendered as org markup instead of plain text.

** Zoho OAuth setup (one time)
Where to click, start to finish:

1. Open the *Zoho API Console*: https://api-console.zoho.com
   (sign in with the same Zoho account you use for Zoho Desk).
2. Click *Get Started* (first visit) or *Add Client* (top right).
3. Choose the *Self Client* client type and confirm with *Create* /
   *OK*. Open it and copy the *Client ID* and *Client Secret* from the
   *Client Secret* tab.
4. Put the client credentials in =~/.authinfo.gpg=:
   #+begin_example
   machine zoho-desk login client-id password 1000.XXXXXXXX
   machine zoho-desk login client-secret password XXXXXXXX
   #+end_example
5. Switch to the *Generate Code* tab (still inside the Self Client)
   and request a grant code with:
   - Scope:
     =Desk.basic.ALL,Desk.tickets.ALL,Desk.settings.READ,Desk.search.READ,Desk.contacts.READ=
   - Time Duration: 10 minutes (the code is single use and expires)
   - Scope Description: anything, e.g. =emacs=
   Click *Create* and copy the code from the popup.
6. In Emacs, run =M-x zoho-desk-authorize= within those 10 minutes and
   paste the code. When it asks =Save refresh token to ~/.authinfo?=,
   answer yes — it appends the =refresh-token= line for you. That is
   the last grant code you will ever generate on this account.

The refresh token does not expire; short-lived access tokens are
minted from it automatically. If you ever see the API console again,
it means the refresh-token line is missing from your authinfo file —
generating a code every session is NOT part of the normal flow.

*** Additional computers
Nothing needs to be redone in the API console: copy the same three
=machine zoho-desk ...= lines into =~/.authinfo.gpg= on each machine
and it works. The refresh token is valid from any number of machines;
each Emacs mints its own access token from it.

Only if you prefer a separate token per machine (e.g. to be able to
revoke one laptop later): repeat steps 5–6 on the new machine, reusing
the same Client ID/Secret. Revoking is done from the API console by
deleting the Self Client (which kills all its tokens).

** Configuration
- =zoho-desk-base-url= / =zoho-desk-accounts-url= :: change if your org
  is on another data center (e.g. =desk.zoho.eu= / =accounts.zoho.eu=).
- =zoho-desk-org-id= :: only needed if the token can see several orgs.
- =zoho-desk-department-id= :: views are per-department in Zoho Desk;
  when unset the dashboard prompts once per session (~D~ in the sidebar
  switches). Set it to skip the prompt.
- =zoho-desk-starred-view-names= :: names shown in the Starred Views
  section. The public API does not expose the web UI's stars, so the
  section is driven by this list. Note the web UI renames system views:
  "My Tickets"/"All Tickets" are =My Cases=/=All Cases= in the API.
- =zoho-desk-comments-public= :: post comments visible to the contact.
- =zoho-desk-sidebar-width= :: width of the views pane.
- =zoho-desk-thread-prefetch= :: how many of the newest emails are
  fetched in full when a ticket opens (older ones load on demand).

Sidebar counts show how many tickets have been fetched for a view
(=50+= means a full page — the API has no per-view total).

* Key bindings

All global commands live under the shared zoho prefix ~SPC a z~,
tickets on ~SPC a z t~ (the =zoho-projects= layer sits next door on
~SPC a z p~):

| Key Binding   | Description                                |
|---------------+--------------------------------------------|
| ~SPC a z t d~ | Open the dashboard                         |
| ~SPC a z t u~ | Open a ticket from its URL (auto-switches department) |
| ~SPC a z t f~ | Quickfind: full ticket number jumps, else helm fuzzy find |
| ~SPC a z t a~ | One-time OAuth grant-code exchange         |
| ~SPC a z t t~ | Add a time entry to a ticket               |
| ~SPC a z t s~ | Set a ticket's status (picklist from the API) |
| ~SPC a z t l~ | Log org-clocked time of the entry at point |
| ~SPC a z t i~ | Start the ticket timer (needs a ticket at point) |
| ~SPC a z t o~ | Finish the ticket timer → fill its New Time Log |
| ~SPC a z t k~ | Cancel the ticket timer                    |

Views sidebar (left pane):

| Key             | Description                       |
|-----------------+-----------------------------------|
| ~RET~ / ~x~ / click | Toggle view checkbox (OR filter) |
| ~g~ / ~gr~      | Refetch views from Zoho           |
| ~s~             | Collapse/reopen the sidebar       |
| ~q~             | Close dashboard, restore windows  |

Ticket table (right pane):

| Key   | Description                          |
|-------+--------------------------------------|
| ~RET~ | Open ticket as org document          |
| ~g~ / ~gr~ | Refresh                         |
| ~]~ / ~[~ | Next / previous page             |
| ~t~   | Add time entry to ticket at point    |
| ~T~   | Start the ticket timer on ticket at point |
| ~u~   | Set the status of ticket at point    |
| ~w~   | Copy org heading with ZOHO_TICKET_ID |
| ~o~   | Open ticket in browser               |
| ~s~   | Collapse/reopen the views sidebar    |
| ~q~   | Close dashboard, restore windows     |

Ticket document (org buffer, =zoho-desk-ticket-minor-mode=). The
buffer is read-only except the Reply and New Time Log input fields;
the header-line tabs are clickable. Commands are on the major-mode
leader (~,~ or ~SPC m~) as well as =C-c z=:

| Leader key | Also         | Description                              |
|------------+--------------+------------------------------------------|
| ~, 1-4~    | ~C-c z 1-4~, click | Overview / Thread / Comments / Time Logs tab |
|            | ~gt~ / ~gT~  | Next / previous tab (evil)               |
| ~, e~      | ~C-c z e~    | Fetch full email body at point           |
| ~, s~      | ~C-c z s~    | Send the Reply section as email          |
| ~, l~      | ~C-c z l~    | Post the New Time Log section            |
| ~, .~      | ~C-c .~      | Org date picker for the Executed / End fields |
|            | ~M-n~ / ~M-p~ | Next / previous input field              |
| ~, c~      | ~C-c z c~    | Compose comment (~, s~ posts, ~, k~ aborts) |
| ~, t~      | ~C-c z t~    | Add time entry                           |
| ~, T~      | ~C-c z T~    | Start the ticket timer on this ticket    |
| ~, u~      | ~C-c z u~    | Set the ticket's status                  |
| ~, w~      | ~C-c z w~    | Copy org heading with ZOHO_TICKET_ID     |
| ~, y~      | ~C-c z y~    | Copy agent-console ticket URL            |
| ~, #~      | ~C-c z #~    | Copy ticket number (=#ART-364= form)     |
| ~, a~      | ~C-c z a~    | Add org contact email to the To line     |
| ~, i e~    | ~C-c z i~    | Insert clipboard image into the reply    |
| ~, i k~    | ~C-c z k~    | Refresh the inline-image session cookie  |
| ~, b~      | ~C-c z o~    | Open in browser                          |
| ~, r~      | ~C-c z g~    | Refresh ticket                           |

The dashboard panes have leader keys too: sidebar ~, t~ toggle /
~, r~ refresh / ~, d~ department / ~, q~ quit; table ~, o~ open /
~, n~ / ~, p~ pages / ~, t~ time / ~, w~ snippet / ~, y~ url /
~, #~ number / ~, b~ browser.

Replies: the Email Thread section lists emails newest-first, with a
=** Reply= subheading at the top — everything under that subheading
(and only that) is editable and is what gets sent. Outgoing replies
are linked to the ticket's newest email thread (undocumented
=inReplyToThreadId= sendReply field), so they continue the
recipient's existing mail conversation instead of starting a new
one, matching what the web UI does. The Reply section starts with an
editable =To:= line prefilled with the sender of the latest inbound
email (fallback: the ticket contact) — the =To: = label itself is
read-only, the address is yours to edit (any address works — send a
test to yourself). The two input fields — the address and the reply
body below it — carry a background halfway between the default and
solaire backgrounds (=zoho-desk-input= face), so the editable areas
are visible at a glance; the body field grows with the reply as you
type. Org's own fontification keeps precedence inside the body, so
src and quote blocks stay on their darker block background. ~, s~ sends straight to it, no prompts; leave the line blank
to pick from the ticket's known addresses instead. ~, a~ (or
~C-c z a~) fuzzy-picks an email from the whole org's contact list
and adds it to the To line (comma-appended when one is already
there); the list is fetched once per session, ~C-u , a~
re-fetches. It needs the =Desk.contacts.READ= scope — tokens
generated before it joined the scope list above must be re-created
via =M-x zoho-desk-authorize= with a fresh grant code. Set
=zoho-desk-signature= to append a plain-text signature to every
reply.

Replies are written in org and exported to HTML on send
(=zoho-desk-reply-html=, default on): bold, lists, tables and links
all render in the email. =#+begin_quote= blocks arrive as italicized
left-bordered blocks and =#+begin_src= blocks as monospace boxes with
syntax highlighting (htmlize inline colors from the current theme, on
a background matching it). Zoho preserves these tags and inline
styles. ~, i e~ (or ~C-c z i~) embeds the image
currently in the clipboard (screenshot it, then ~, i e~); with a
prefix argument it prompts for a file instead. The image lands in
the reply as a =[[file:...]]= link (shown inline in the buffer);
on send it is uploaded through the agent console's composer
servlet and sent *truly inline* — Zoho's mailer converts the
composer URL into a real cid MIME part, so the recipient sees the
image in the email body (=zoho-desk-send-inline-images=, default
on; when nil, images fall back to regular attachments with
=[image: ... — attached]= markers, since Zoho strips =cid:= and
=data:= images from plain API sends). Set
=zoho-desk-reply-html= to nil for plain-text replies.

Inline sending needs the browser's agent-console session cookie —
the composer servlet has no OAuth equivalent. When the cookie is
missing or expired, the send is *refused* (nothing goes out, the
reply stays put): the ticket's agent console opens in your browser
and the minibuffer prompts for a refresh. In the browser, first
make sure the =/agent/= console is actually *signed in* — a fresh
browser session lands on the customer-portal ticket view instead,
and its visitor cookies look plausible but the servlet refuses
them — then reload and =F12= → Network tab → click a fresh request
to the support host → Request Headers → copy the whole =Cookie:=
line and paste it at the prompt. The complete header is saved to
authinfo after checking that =__Secure-iamsdt= and =crmcsr= are
present and nonempty. Additional session and routing cookies are
preserved. Authentication is checked when the composer uploads an
image; the console page is not a reliable test of upload access.
Quitting the prompt just aborts the send with a reminder. ~, i k~
(or ~C-c z k~, =zoho-desk-refresh-session-cookie=) runs the same
refresh proactively.

Refresh also prompts for the =User-Agent= line from the *same request*.
The cookie and browser identity are saved together in authinfo and
restored after restarting Emacs. No browser or platform is hardcoded.
When switching browsers or after an update changes its User-Agent,
refresh both values. A cookie-only legacy session prompts for refresh
before uploading. =zoho-desk-browser-user-agent= is an optional fallback
for legacy sessions; normally leave it nil.

Images that are already in the ticket need none of that: an
incoming email's inline images display in the thread as
=[[/api/v1/.../inlineImages/...]]= links, and yanking such a link
into your reply re-sends that image truly inline over plain
OAuth. Use it to quote a customer's screenshot back with
commentary.

Time logs: the Time Logs tab lists the ticket's time entries as
org subheadings, newest as returned by the API, each carrying its
timestamp, duration, owner and a description snippet in the
heading. Entries are *collapsed by default* — every tab switch
re-folds them — so the tab reads as a compact index; ~TAB~ on a
heading opens the details (full description, charge type, costs).
The top entry is =** New Time Log=: unfold it and you get the same
input-field pattern as the Reply section — an =Executed:= line
prefilled with the current time (~C-c .~ or ~, .~ opens the org
date picker on it, or edit the timestamp by hand; blank means
now), an =End:= line (blank by default; filled, the entry is the
Executed..End interval and the duration is computed from it), a
duration line with =Hours:= / =Minutes:= / =Seconds:= values
(blank counts as 0; the End time's alternative — giving both is
refused), and a description field below them, all on the
=zoho-desk-input= background. ~M-n~ / ~M-p~ cycle
between the input fields (works in normal and insert state; ~TAB~
keeps its org folding role). ~, l~ (or ~C-c z l~) posts it in the
background and refreshes the ticket so the new entry appears in
the list.

Ticket timer: ~T~ on a ticket (table line or open ticket, also
~, T~ / ~C-c z T~ / ~SPC a z t i~) clocks the =posframe-timer=
free clock in against it — the floating clock counts up with the
ticket number and subject above it, so what you're billing is
always on screen. The timer follows the ticket, not the buffer:
open other tickets, close the buffer, work anywhere. When done,
~SPC a z t o~ (or =posframe-timer='s own clock-out, ~SPC a T o~)
stops the clock from wherever you are, brings the ticket up on
its Time Logs tab and fills the New Time Log for you — Executed
and End set to the clocked start and end times (the duration is
computed from them at submit), point in the description field.
Zoho Desk stores a time entry as its start plus a duration, so
the cataloged interval survives in full. Describe the
work and ~, l~ submits as usual. ~SPC a z t k~ (or ~SPC a T k~)
discards the timer without logging anything. Requires the
=posframe-timer= layer.

The From address is resolved per department: global
=zoho-desk-reply-from= → =zoho-desk-department-reply-from= entry →
address learned this session → this ticket's outbound/inbound
emails → a one-time prompt that offers to remember the answer.

Email replies: write in the body field below the =To:= line under
the =** Reply= heading (only the field's content is sent; org =#=
comment lines are stripped from the email) and ~C-c z s~ sends in
the background.  From is the support
address the latest inbound email was sent to, unless
=zoho-desk-reply-from= is set.  Web-form tickets often have no
inbound email, so set =zoho-desk-reply-from= to your department's
support address if sending fails.

* Org workflow
1. In the ticket table, press ~w~ on a ticket and yank into an org file:
   #+begin_example
   * TODO #ART-359 Deploy Updated PTC Hub to Dev
   :PROPERTIES:
   :ZOHO_TICKET_ID: 4000000031007
   :END:
   #+end_example
2. Clock time on the entry as usual.
3. =M-x zoho-desk-log-time-from-org= (~SPC a z t l~) on the entry posts the
   clocked minutes as a Zoho Desk time entry.

* API notes
Endpoints used (base =https://desk.zoho.com/api/v1=, headers
=Authorization: Zoho-oauthtoken <token>= and =orgId=):

- =GET /organizations= — resolve orgId
- =GET /starredViews?module=tickets= (fallback =GET /views=)
- =GET /tickets?viewId=&include=contacts,assignee&limit=&from= — fetched
  once per checked view, then merged (union, deduped by ticket id,
  newest modification first)
- =GET /tickets/{id}= , =/threads=, =/threads/{tid}=, =/comments=,
  =/timeEntry=
- =POST /tickets/{id}/comments= — ={"content": ..., "isPublic": ...}=
- =POST /tickets/{id}/timeEntry= — ={"hoursSpent", "minutesSpent",
  "secondsSpent", "executedTime", "description"}=
- =GET /organizationFields?module=tickets= — the =status= field's
  =allowedValues= is the status picklist (fetched once per session)
- =PATCH /tickets/{id}= — ={"status": ...}= to set a ticket's status

Full reference: https://desk.zoho.com/DeskAPIDocument
